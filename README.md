# PPGModifier

To depoy run this from commandline in the project directory:
dotnet publish -c Release -r win-arm64 --self-contained true -p:PublishSingleFile=true
dotnet publish -c Release -r win-x64 --self-contained true
